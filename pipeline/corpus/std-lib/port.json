{"status": "ok", "mode": "benchmark", "verdict": "ported", "blocker": "", "decode_layout": "bytes[0..4] = i32 little-endian = n (4 bytes total). Input shorter than 4 bytes returns an empty Vec without panicking.", "entry_called": "lifted body of stdlib::string_concat(n: i32) -> String; concatenates decimal strings of 0..n, returns res.into_bytes()", "files_changed": ["guest/src/lib.rs"]}