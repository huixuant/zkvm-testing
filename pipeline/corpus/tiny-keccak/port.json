{"status": "ok", "mode": "crate", "verdict": "ported", "blocker": "", "decode_layout": "input: Vec<u8> is used verbatim as the message to hash (variable length 0..=65536 bytes, no header/prefix). Decode is total: empty input hashes the empty message. Output: 32-byte SHA3-256 digest as raw bytes.", "entry_called": "tiny_keccak::Sha3::v256() then Hasher::update(&input) and Hasher::finalize(&mut [u8;32])", "files_changed": ["guest/Cargo.toml", "guest/src/lib.rs"]}