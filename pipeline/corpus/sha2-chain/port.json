{"status": "ok", "mode": "benchmark", "verdict": "ported", "blocker": "", "decode_layout": "36 bytes total, little-endian: [0..32] = input seed hash ([u8;32]); [32..36] = num_iters (u32 LE). If input.len() < 36, harness returns an empty Vec (total decode, no panic). Output = the 32 raw bytes of the final [u8;32] hash.", "entry_called": "jolt_inlines_sha2::Sha256::digest(&hash) iterated num_iters times (lifted sha2_chain body)", "files_changed": ["guest/Cargo.toml", "guest/src/lib.rs"]}