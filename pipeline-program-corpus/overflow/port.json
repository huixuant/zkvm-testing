{"status": "ok", "mode": "benchmark", "verdict": "ported", "blocker": "", "decode_layout": "No input consumed. The corpus `overflow` provable functions take no arguments; the lifted computation (overflow_stack) is a fixed stack-array reduction with no operands. `input: Vec<u8>` is accepted and ignored for any length (decode is total, no panic). Output: u32 result serialised as 4 little-endian bytes (always 1024 = 0x00040000 -> bytes 00 04 00 00).", "entry_called": "lifted body of overflow::guest::overflow_stack -> let arr=[1u32;1024]; arr.iter().sum::<u32>()", "files_changed": ["guest/src/lib.rs"]}